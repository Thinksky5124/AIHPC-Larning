
file(GLOB_RECURSE CPP_SOURCES "cc/*.cpp" "cc/*.cc")
file(GLOB_RECURSE CPP_HEADERS "cc/*.h" "cc/*.hpp")

set(CORE_SRCS ${CORE_SRCS} ${CPP_SOURCES})
set(CORE_INCLUDE_DIR ${CORE_INCLUDE_DIR} ${CMAKE_CURRENT_LIST_DIR}/cc)

set(CORE_SRCS ${CORE_SRCS} PARENT_SCOPE)
set(CORE_INCLUDE_DIR ${CORE_INCLUDE_DIR} PARENT_SCOPE)